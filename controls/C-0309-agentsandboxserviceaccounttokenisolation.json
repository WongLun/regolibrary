{
    "name": "Agent Sandbox service account token isolation",
    "attributes": {
        "controlTypeTags": [
            "security"
        ]
    },
    "description": "Ensure Agent Sandbox workloads do not receive Kubernetes service account tokens unless an operator deliberately accepts that trust-boundary exception.",
    "remediation": "Set spec.podTemplate.spec.automountServiceAccountToken to false on direct Sandbox resources. SandboxTemplate resources may omit the field because the template controller securely defaults it to false, but must never set it to true.",
    "rulesNames": [
        "agent-sandbox-service-account-token"
    ],
    "long_description": "Agent Sandbox workloads commonly execute untrusted or model-generated code. Mounting a Kubernetes service account token gives that code an identity it can use against the Kubernetes API. Direct Sandbox resources therefore need an explicit false value. SandboxTemplate resources are secure when the value is omitted because the Agent Sandbox template controller defaults it to false; this control preserves that API distinction instead of reporting a false positive.",
    "test": "Check that direct Sandbox resources explicitly disable service account token automounting and that SandboxTemplate resources do not explicitly enable it.",
    "controlID": "C-0309",
    "baseScore": 7.0,
    "category": {
        "name": "Workload"
    },
    "scanningScope": {
        "matches": [
            "cluster",
            "file"
        ]
    }
}
