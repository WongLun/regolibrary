apiVersion: ate.dev/v1alpha1
kind: WorkerPool
metadata:
  name: worker-allowed
spec:
  replicas: 1
  workerImage: gcr.io/team/actor
  template:
    resources:
      limits:
        cpu: '1'
        memory: 256Mi
