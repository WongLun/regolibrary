apiVersion: ate.dev/v1alpha1
kind: WorkerPool
metadata:
  name: worker-host
spec:
  replicas: 1
  workerImage: evil.example/nginx
  template:
    resources:
      limits:
        cpu: '1'
        memory: 256Mi
