apiVersion: ate.dev/v1alpha1
kind: WorkerPool
metadata:
  name: worker-docker-path
spec:
  replicas: 1
  workerImage: library/nginx
  template:
    resources:
      limits:
        cpu: '1'
        memory: 256Mi
