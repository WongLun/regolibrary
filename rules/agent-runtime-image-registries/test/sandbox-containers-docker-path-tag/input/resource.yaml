apiVersion: agents.x-k8s.io/v1beta1
kind: Sandbox
metadata:
  name: sandbox-containers-docker-path-tag
spec:
  podTemplate:
    spec:
      runtimeClassName: gvisor
      containers:
      - name: actor
        image: org/nginx:1.0
        resources:
          limits:
            cpu: '1'
            memory: 256Mi
