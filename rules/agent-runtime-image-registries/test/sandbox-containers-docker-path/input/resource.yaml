apiVersion: agents.x-k8s.io/v1beta1
kind: Sandbox
metadata:
  name: sandbox-containers-docker-path
spec:
  podTemplate:
    spec:
      runtimeClassName: gvisor
      containers:
      - name: actor
        image: library/nginx
        resources:
          limits:
            cpu: '1'
            memory: 256Mi
