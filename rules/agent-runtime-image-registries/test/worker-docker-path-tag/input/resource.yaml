apiVersion: ate.dev/v1alpha1
kind: WorkerPool
metadata:
  name: worker-docker-path-tag
spec:
  replicas: 1
  workerImage: org/nginx:1.0
  template:
    resources:
      limits:
        cpu: '1'
        memory: 256Mi
