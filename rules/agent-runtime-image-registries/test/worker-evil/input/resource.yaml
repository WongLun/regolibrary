apiVersion: ate.dev/v1alpha1
kind: WorkerPool
metadata:
  name: worker-evil
spec:
  replicas: 1
  workerImage: gcr.io.evil/actor
  template:
    resources:
      limits:
        cpu: '1'
        memory: 256Mi
