apiVersion: ate.dev/v1alpha1
kind: WorkerPool
metadata:
  name: worker-docker-short
spec:
  replicas: 1
  workerImage: nginx
  template:
    resources:
      limits:
        cpu: '1'
        memory: 256Mi
